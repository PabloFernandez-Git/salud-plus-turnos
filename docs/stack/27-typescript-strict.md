# 27. TypeScript strict

El proyecto utilizará:

```text
strict: true
```

Esto hace que TypeScript sea más exigente con posibles errores y estados inválidos.

En un dominio con muchas relaciones y estados como este gestor de turnos, preferimos detectar inconsistencias durante el desarrollo antes que descubrirlas en producción.
