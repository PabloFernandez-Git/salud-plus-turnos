# Domain modules

Los módulos se crean cuando una tarea real introduce funcionalidad. No se crean carpetas vacías por anticipado.

Estructura orientativa cuando un módulo crece:

```text
appointments/
├── components/
├── schemas/
├── queries/
├── actions/
└── domain/
```
